{
  "name": "grove",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ps:build": "spago build",
    "ps:test": "spago test",
    "clean": "node scripts/clean.mjs",
    "bundle": "node scripts/bundle.mjs",
    "copy-static": "node scripts/copy-static.mjs",
    "build": "node scripts/clean.mjs && spago build && node scripts/bundle.mjs && node scripts/copy-static.mjs",
    "package": "pnpm run build && web-ext build --source-dir dist --artifacts-dir web-ext-artifacts --overwrite-dest",
    "package:source": "node scripts/package-source.mjs",
    "test": "spago test",
    "test:e2e": "playwright test",
    "lint:ext": "web-ext lint --source-dir dist --no-config-discovery",
    "check": "node scripts/clean.mjs && spago test && spago build && node scripts/bundle.mjs && node scripts/copy-static.mjs && web-ext lint --source-dir dist --no-config-discovery && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "esbuild": "^0.28.1",
    "purescript": "0.15.15",
    "spago": "^1.0.4",
    "web-ext": "^10.4.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "purescript",
      "spago",
      "esbuild"
    ]
  },
  "packageManager": "pnpm@10.28.1+sha512.7d7dbbca9e99447b7c3bf7a73286afaaf6be99251eb9498baefa7d406892f67b879adb3a1d7e687fc4ccc1a388c7175fbaae567a26ab44d1067b54fcb0d6a316"
}
